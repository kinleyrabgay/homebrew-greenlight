# homebrew-greenlight

Homebrew tap for [greenlight](https://github.com/kinleyrabgay/greenlight) — a local git proxy that validates code before pushing upstream.

```sh
brew install kinleyrabgay/greenlight/greenlight
```

Builds from source (requires the `go` toolchain at install time).
